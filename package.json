{
  "name": "lychee-ripe",
  "private": true,
  "packageManager": "bun@1.2.23",
  "workspaces": [
    "clients/*",
    "services/*",
    "mlops/*",
    "shared/contracts"
  ],
  "scripts": {
    "dev": "bun ./tooling/scripts/turbo-task.mjs dev --parallel --filter=@lychee-ripe/inference-api --filter=@lychee-ripe/gateway --filter=@lychee-ripe/orchard-console",
    "dev:inference-api": "bun ./tooling/scripts/turbo-task.mjs dev --filter=@lychee-ripe/inference-api",
    "dev:gateway": "turbo run dev --filter=@lychee-ripe/gateway",
    "dev:orchard-console": "turbo run dev --filter=@lychee-ripe/orchard-console",
    "build": "turbo run build",
    "test": "bun ./tooling/scripts/turbo-task.mjs test",
    "test:stack": "bun tests/stack/smoke-stack.mjs",
    "typecheck": "turbo run typecheck",
    "generate": "turbo run generate",
    "verify": "bun ./tooling/scripts/turbo-task.mjs verify"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
